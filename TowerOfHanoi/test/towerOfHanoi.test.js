describe("game rules", () => {
  specify("only one disk can be moved at a time", () => {});
  specify(
    "only the the upper disk can be moved from one stack to another stack",
    () => {}
  );
  specify("a larger disk cannot be placed on top of a smaller disk", () => {});
});

describe("win condition", () => {
  specify("all disks are on the winning rod", () => {});
});
